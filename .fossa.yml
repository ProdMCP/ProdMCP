version: 3

server: https://app.fossa.com

project:
  id: prodmcp
  name: ProdMCP
  url: https://github.com/ProdMCP-org/ProdMCP
  policy: managed-audited

paths:
  exclude:
    - .venv/
    - .local/
    - dist/
    - build/
    - .pytest_cache/
    - docs/
